
<?php if (!empty($playlist) && isset($playlist[0])): ?>

<h1><?= $playlist[0]->nombre_playlist ?></h1>

<ul>
<?php foreach($playlist as $cancion): ?>

    <li>
        <?= $cancion->nombre_cancion ?>
    </li>

<?php endforeach; ?>
</ul>

<?php else: ?>

    <h1>No hay datos</h1>

<?php endif; ?>

<a href="<?= BASE_URL ?>home">VOLVER</a>